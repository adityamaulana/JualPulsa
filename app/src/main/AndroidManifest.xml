<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.jualpulsa">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.JualPulsa">
        <activity android:name=".SelesaiTransaksi"></activity>
        <activity android:name=".KonfirmasiPembayaranPulsa" />
        <activity android:name=".MetodePembayaranPulsa" />
        <activity android:name=".Pulsa" />
        <activity android:name=".MenuUtama" />
        <activity android:name=".Daftar" />
        <activity android:name=".Login" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>